# PROJECT Я — APP-TEMPLATE-0.1

**Signature:** RIZALBOT🤖 / CoS  
**Date:** 2026-09-15 22:19:29 MDT (America/Denver)  
**Parent aesthetic:** AESTHETIC-TEMPLATE-CLAY-2026-09-15  
**Live seat (intended):** `/Users/rizal/Documents/ЯBOT/`

## Status note — COMMAND RESPAWN
**RESPAWN was pending at pack time.** Box mirrors of `CompanionRouter.swift` did not contain `respawn` after a 60s recheck. This executor subagent Shell schema has no `machineId`, so live Mac path `/Users/rizal/Documents/ЯBOT/ЯBOT/CompanionRouter.swift` could not be verified. Parent should confirm RESPAWN on Mac (machineId `55d92eb0-0ce0-4959-b297-440d80e59e38`) and re-pack source if the live router landed after this stamp.

## Laws (locked winner — Stormclay White/Gold)

1. **Claymation everywhere** — digital clay / plasticine UI; not flat Material/glass plates.
2. **Stormclay White / Gold** — chat type uses **Stormclay White**; system/chrome type uses **Stormclay Gold**. Bundle `Fonts/StormclayWhite.ttf`, `Fonts/StormclayGold.ttf`, and `Fonts/Stormclay.ttf` (+ OFL).
3. **Rectangular 4-corner tablets** — chat tablets are rectangular with four corners; **never pointed** (no speech-bubble tails / pointed corners).
4. **fixed fontChat 16** — chat body text size is locked at **16**.
5. **Alignment** — user tablets on the **right**; companion tablets on the **left**; text **left-aligned** inside tablets.
6. **3-part composer** — long clay bar containing: (a) + / add, (b) text field, (c) solid **Send** control **inside** the long bar (not floating outside).
7. **COMMAND RESPAWN** — short-order `respawn` restores / reloads from the official most-recent app template via `ya-respawn.sh` / CompanionRouter (when seated). See status note above if pending at pack time.
8. **Grok Bot alignment** — CoS / Grok Bot answer-first; Decider short orders; NonNuclear; offline seat wins.

## Pack layout
- `APP-CONTRACT-0.1.md` — this contract
- `OFFICIAL.json` — registry identity
- `PROVENANCE.txt` — stamp + signature + parent
- `MANIFEST.txt` — file list + sizes
- `Assets.xcassets/` — live-ish clay assets
- `Fonts/` — StormclayWhite, StormclayGold, Stormclay, OFL
- `source/` — all `.swift` + `Info.plist`
- `source-project/ЯBOT/` — restore mirror for `ya-respawn.sh`
- `ya-respawn.sh` — official respawn restore script
- `AESTHETIC-WINNER-stormclay-3d-font-pack.png` — when present

## Restore
```bash
/Users/rizal/Documents/ЯBOT/ya-respawn.sh
# or older:
/Users/rizal/Documents/ЯBOT/ya-respawn.sh AESTHETIC-TEMPLATE-CLAY-2026-09-15
```

RIZALBOT🤖 / CoS waypoint
